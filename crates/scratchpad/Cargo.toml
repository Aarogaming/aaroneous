[package]
name = "scratchpad"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
eframe = { version = "0.34", default-features = false }
egui = "0.34"
api = { path = "../api" }

serde = { version = "1.0", features = ["derive"] }
