[package]
name = "nlp_edge"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
state_serialization = { path = "../state_serialization" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Use existing router client for LLM calls
"../dev/tools/afc" = { path = "../../dev/tools/afc" }
