[package]
name = "plugin_api"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
ipc_bus = { path = "../../crates/ipc_bus" }

[lib]
path = "src/lib.rs"
