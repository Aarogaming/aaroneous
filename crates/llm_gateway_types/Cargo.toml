[package]
name = "llm_gateway_types"
version = "0.1.0"
edition.workspace = true
authors = ["Aaron"]
description = "Sync-safe type definitions for LLM gateway configuration, model registry, and analysis results"
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
