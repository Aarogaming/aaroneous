[package]
name = "nat_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# nats: synchronous NATS client for publishing federation events from the enzyme
nats = "0.26"
serde_json = "1.0"
