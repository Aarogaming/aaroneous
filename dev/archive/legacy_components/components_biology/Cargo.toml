[package]
name = "biology"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sysinfo = "0.30"
strum = { version = "0.26", features = ["derive"] }
tracing = "0.1"
anyhow = "1.0"
rand = "0.8"
compute = { path = "../compute" }
