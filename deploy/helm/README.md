# Aaroneous Helm Reference Blueprint

> **Notice:** The canonical Aaroneous runtime is a sovereign, machine-native single-node or P2P mesh deployment using SQLite (`hive.db`) and lock-free SPMC shared memory.
>
> This Helm chart is provided as an enterprise reference design for deploying containerized headless hypervisors into Kubernetes clusters.
